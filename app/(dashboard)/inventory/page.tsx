import {products} from '@/lib/demo-data'; export default function Inventory(){const low=products.filter(p=>p.stock<p.minimumStock);return <><div className="topbar"><div><div className="title">Inventory</div><div className="muted">Products, stock levels and alerts</div></div><div style={{display:'flex',gap:8}}><button className="btn">Stock In</button><button className="btn">Stock Out</button></div></div><div className="grid grid-4" style={{marginBottom:16}}><div className="card"><div className="label">Products</div><div className="stat">{products.length}</div></div><div className="card"><div className="label">Low Stock</div><div className="stat">{low.length}</div></div></div><div className="card"><table><thead><tr><th>Product</th><th>Category</th><th>Stock</th><th>Minimum</th><th>Unit</th><th>Status</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td><b>{p.name}</b></td><td>{p.category}</td><td>{p.stock}</td><td>{p.minimumStock}</td><td>{p.unit}</td><td>{p.stock<p.minimumStock?<span className="badge">LOW STOCK</span>:<span className="badge">NORMAL</span>}</td></tr>)}</tbody></table></div></>}
