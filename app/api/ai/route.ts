import {NextResponse} from 'next/server'; import {z} from 'zod'; const bodySchema=z.object({message:z.string().min(1).max(2000)});
export async function POST(req:Request){try{const parsed=bodySchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Invalid request.'},{status:400});if(!process.env.GEMINI_API_KEY)return NextResponse.json({mode:'demo',message:'Gemini integration is not configured. Add GEMINI_API_KEY on the server to enable real AI.'});return NextResponse.json({mode:'integration-ready',message:'AI provider is configured. Implement provider call here using the server-side secret and hotel data service.'});}catch{return NextResponse.json({error:'Unable to process request.'},{status:500})}}
